## 1. Baseline Reconciliation

- [x] 1.1 Update `cli-and-config` to describe the implemented MVP CLI surface
- [x] 1.2 Update `cli-and-config` role detection semantics to match current behavior
- [x] 1.3 Update `cli-and-config` output and mode semantics to match current behavior
- [x] 1.4 Update `universal-code-model` to describe the current parser/query/diagnostic behavior
- [x] 1.5 Remove unsupported or unimplemented baseline claims from both specs
- [x] 1.6 Validate the change with `openspec validate update-mvp-spec-baseline --strict`
