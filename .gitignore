
# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key

# Beads internal state (track only issues.jsonl for backup)
.beads/*
!.beads/issues.jsonl
!.beads/config.yaml

# Pretender runtime cache
pretender/.pretender/

# mdBook build output
/book

# Vale downloaded styles (run `vale sync` to populate)
.vale/styles/
