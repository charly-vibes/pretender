pre-commit:
  parallel: true
  commands:
    fmt:
      run: just fmt-check
    type-check:
      run: just type-check
    lint:
      run: just lint

pre-push:
  commands:
    test:
      run: just test
