pre-commit:
  parallel: true
  commands:
    type-check:
      run: just type-check
    lint:
      run: just lint

pre-push:
  parallel: true
  commands:
    fmt:
      run: just fmt-check
    test:
      run: just test
